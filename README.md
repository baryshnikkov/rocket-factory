# microservices-rocket-factory

---

## ⚙️ Требования
Перед использованием команд убедитесь, что установлены:
- **Go 1.24+**
- **Taskfile** — [установка](https://taskfile.dev/installation/)
- **Git**

---

## 🔧 Команды для разработки

В проекте используется [Taskfile](https://taskfile.dev/) для автоматизации рутинных действий. Все команды запускаются через `task <имя-задачи>`.

### 📋 Сводная таблица команд

| Команда                | Описание                                                                                  |
|------------------------|-------------------------------------------------------------------------------------------|
| `task format`          | Форматирование кода (`gofumpt` + `gci`, исключая `mocks`)                                 |
| `task lint`            | Линтинг кода во всех модулях (`golangci-lint`, включая проверки безопасности `gosec`)     |
| `task deps:update`     | Обновление зависимостей (`go.work sync` + `go mod tidy -compat=1.24`)                     |
| `task ogen:gen`        | Генерирует Go-код из OpenAPI-деклараций с `x-ogen` (сначала бандлит схемы через `redocly`) |
| `task proto:lint`      | Проверяет .proto-файлы на стиль и ошибки (buf lint)                                       |
| `task proto:gen`       | Генерирует Go-код из .proto-файлов с помощью buf generate                                 |
| `task gen`             | Выполняет общую генерацию всех исходников: и proto, и OpenAPI (ogen)                      |
| `task grpcurl:install` | Устанавливает CLI-инструмент grpcurl для вызова gRPC-методов (аналог curl для gRPC)       |
| `task run`             | Запускает все микросервисы (inventory, payment, order)                                    |
| `task run:inventory`   | Запускает inventory микросервис                                                           |
| `task run:payment`     | Запускает payment микросервис                                                             |
| `task run:order`       | Запускает order микросервис                                                               |
| `task stop`            | Останавливает все запущенные микросервисы                                                 |